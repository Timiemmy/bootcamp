num = [10, 20, 30, 40]
name = ['hello', 'how', 'dane']

# Traversing over all data and changing values
# Range and index is used while trying to traverse and change a data
for index in range(len(num)):
    num[index] *= 2

print(num)